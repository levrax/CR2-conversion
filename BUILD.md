# Сборка CR2-конвертера

Короткая инструкция: как собрать приложение руками, что делает CI и как выпустить релиз.

---

## 1. Сборка на Windows

Нужен Python 3.12 (64-bit).

```bat
python -m venv .venv
.venv\Scripts\activate
python -m pip install -r requirements.txt
python -m pip install pyinstaller
python -m unittest
pyinstaller --noconfirm --clean cr2_gui.spec
```

Результат: `dist\CR2Converter\CR2Converter.exe` плюс папка `dist\CR2Converter\_internal\`
(режим onedir, около 72 МБ). Раздавать надо **всю папку целиком**, а не только `.exe`.

Почему onedir, а не один файл: замеры на этой машине дали запуск 0,23-0,32 с для onedir
против 1,33-1,44 с для onefile. Onefile каждый раз распаковывает ~70 МБ во временную папку,
и эта секунда с лишним платится при **каждом** запуске. Для окна на tkinter это разница
между «открылось сразу» и «я вообще попал по ярлыку?».

Собирайте в чистом venv. Глобальный `site-packages` утягивает в бандл лишнее
(pywin32, yaml, charset_normalizer и прочее) — это даёт больше мусора, чем любые `excludes`.

---

## 2. Сборка на Mac

Нужен Python 3.12 и Xcode Command Line Tools.

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python -m pip install pyinstaller
python -m unittest
pyinstaller --noconfirm --clean cr2_gui.spec
```

Результат: `dist/CR2Converter.app` (и рядом служебная папка `dist/CR2Converter/`).

**Важно про архитектуры.** У `rawpy` на PyPI есть колёса только под `macosx_11_0_arm64`.
Ни x86_64, ни universal2 нет. Значит:

* на Apple Silicon (M1/M2/M3/M4) всё собирается штатно;
* на Intel-маке `pip install rawpy` не найдёт колесо — либо собирайте LibRaw и rawpy
  сами, либо живите без поддержки RAW через rawpy (остальной функционал работает,
  rawpy импортируется лениво);
* `target_arch='universal2'` собрать нельзя — PyInstaller упадёт с
  `IncompatibleBinaryArchError`. Склеить два бандла через `lipo` тоже не выйдет:
  у каждой архитектурной части свой встроенный PKG-архив, и загрузчик найдёт только один.

Упаковывать `.app` нужно только через `ditto`, не через `zip`:

```bash
ditto -c -k --sequesterRsrc --keepParent dist/CR2Converter.app CR2Converter.zip
```

Внутри `.app` есть симлинки и расширенные атрибуты с подписью — обычный `zip` их теряет,
после чего бандл может вообще не запуститься.

---

## 3. Что делает CI

Файл: `.github/workflows/build.yml`.

Запускается на: любой `push`, любой `pull_request`, пуш тега `v*`, и вручную
(`workflow_dispatch`).

Матрица — три раннера, потому что PyInstaller не умеет кросс-компиляцию
и каждую ОС надо собирать на ней же:

| Раннер           | Платформа      | Артефакт                                   |
|------------------|----------------|--------------------------------------------|
| `windows-latest` | Windows x64    | `CR2Converter-<версия>-windows-x64.zip`    |
| `macos-latest`   | macOS arm64    | `CR2Converter-<версия>-macos-arm64.zip`    |
| `macos-15-intel` | macOS x86_64   | `CR2Converter-<версия>-macos-x86_64.zip`   |

На каждом раннере по шагам: checkout → Python 3.12 → установка `requirements.txt`
и `pyinstaller` → **прогон всех тестов (`python -m unittest`)** → сборка по `.spec` →
упаковка (Windows — zip папки onedir; macOS — `ditto -c -k --keepParent` бандла) →
файл контрольных сумм SHA-256 → выгрузка артефакта.

Если тесты падают — сборки не будет, задача помечается красным. Это намеренно:
тесты стоят перед сборкой, а не после.

Примечание про `macos-15-intel`: образ `macos-13` снят с обслуживания 4 декабря 2025 года,
`macos-15-intel` — штатная замена для x86_64. Intel-образы macOS вообще уходят
примерно к осени 2027, так что Intel-ветка не навсегда.

Второе примечание: на Intel-маке шаг установки зависимостей не уронит сборку из-за
отсутствующего колеса `rawpy` — он поставит остальные пакеты и напишет предупреждение.

---

## 4. Как выпустить релиз

```bash
git tag v1.0.0
git push origin v1.0.0
```

(Пуш делаете вы сами и тогда, когда решите публиковать репозиторий — CI сработает
только в GitHub.)

По тегу `v*` отрабатывает вся матрица, а затем job `release`:
скачивает артефакты всех трёх платформ, сливает контрольные суммы в один
`SHA256SUMS.txt` и создаёт GitHub Release командой `gh release create`
(черновик → заливка файлов → публикация — единственный порядок, который работает
при включённых immutable releases).

У job `release` стоит `permissions: contents: write`; у всего остального workflow —
`contents: read`. Без этого блока `gh release create` вернёт
`403 Resource not accessible by integration`.

---

## 5. Приложение не подписано — что увидят пользователи

Сборки без сертификата. Это не поломка, но выглядит пугающе, и об этом надо
честно писать в описании релиза.

### Windows: SmartScreen

При первом запуске Microsoft Defender SmartScreen покажет синее окно
**«Система Windows защитила ваш компьютер»** с подписью «Неизвестный издатель».
Кнопки «Выполнить» сразу не видно.

Обход: **«Подробнее» → «Выполнить в любом случае»**.

Репутация SmartScreen считается по хешу конкретного файла и по издателю, поэтому
каждая новая сборка начинает с нуля, пока нет подписи.

### macOS: Gatekeeper

Скачанный из браузера `.app` получает атрибут карантина, и macOS скажет
**«Не удалось проверить, что программа не содержит вредоносного ПО»**.

Обход на macOS 15 (Sequoia) и новее — старый «правый клик → Открыть» **больше не работает**:

1. двойной клик по приложению, закрыть предупреждение кнопкой «Готово»;
2. **Системные настройки → Конфиденциальность и безопасность**, прокрутить до раздела
   «Безопасность»;
3. рядом с именем приложения нажать **«Всё равно открыть»** и подтвердить.

Кнопка «Всё равно открыть» появляется примерно на час после заблокированного запуска.
Не успели — запустите приложение ещё раз, чтобы она вернулась.

Для тех, кому проще одной командой:

```bash
xattr -dr com.apple.quarantine /Applications/CR2Converter.app
```

Команду выполняет **получатель после скачивания** — на машине сборки она бессмысленна,
атрибут навешивает браузер при загрузке.

PyInstaller сам ставит ad-hoc подпись на macOS (иначе бинарник не загрузится на Apple
Silicon), но ad-hoc подпись — это **не** нотаризация, Gatekeeper её не принимает.

### Сколько стоит подписать

**macOS.** Членство в Apple Developer Program — **99 $ в год**. Нужен сертификат
Developer ID Application, сборка с hardened runtime, затем нотаризация через
`xcrun notarytool` (старый `altool` Apple не принимает с 1 ноября 2023) и `xcrun stapler
staple`. Только после этого приложение открывается двойным кликом без плясок.
Самоподписанный сертификат не подходит: с hardened runtime приложение упадёт с
`mapped file has no Team ID and is not a platform binary`.

**Windows.** Обычный сертификат для подписи кода: примерно **200-400 $/год** (OV)
и **300-700 $/год** (EV), и с июня 2023 ключ обязан лежать на аппаратном токене или в HSM —
в CI это неудобно. Дешевле Azure Trusted Signing — **9,99 $/месяц**, без токена,
дружит с CI, но доступен только верифицированным компаниям и ИП (США/Канада, ЕС/Великобритания),
для организаций — с историей деятельности от 3 лет.

Отдельно: с августа 2024 Microsoft больше не различает OV и EV для репутации SmartScreen.
Старый совет «купи EV и предупреждение исчезнет сразу» устарел — репутация всё равно
набирается по мере выпуска стабильно подписанных версий.

**Итого:** минимум ~99 $/год за спокойный macOS и ~120 $/год (Azure) или ~200+ $/год
(обычный сертификат) за спокойный Windows. Пока подписи нет — просто напишите в релизе,
как обойти предупреждения.
